# UI -> Server base URL (Vite only exposes VITE_* vars)
VITE_API_BASE=http://127.0.0.1:3033

# Value Bridge banner configuration (optional)
# Base currency the UI assumes if the server hasn't told it otherwise.
VITE_VALUE_BASE_CURRENCY=EUR
# Upcoming currency after transition; set equal to base to disable notice.
VITE_VALUE_NEXT_CURRENCY=EUR
# ISO date when the new currency takes effect (e.g., 2025-11-01). Leave blank for none.
VITE_VALUE_REGIME_CHANGE=
# How many days before the switch the UI should show the notice banner.
VITE_VALUE_NOTICE_WINDOW=45

# Optional: prefill a dev token in localStorage if you’d like
# (The UI reads BEARER from localStorage key "m3_token")
# Example snippet to run once in your browser console:
# localStorage.setItem('m3_token', 'supersecret')
