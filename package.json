{
  "scripts": {
    "prepare": "husky",
    "lint": "pnpm -w -r run lint || true",
    "fmt": "cargo fmt && pnpm -w -r run format || true",
    "check": "cargo clippy --tests -- -D warnings",
    "precommit": "pnpm fmt && pnpm lint && cargo test -q || true",
    "mark": "tsx scripts/new-mark.ts"
  },
  "devDependencies": {
    "@types/node": "^24.5.0",
    "husky": "^9.1.7",
    "tsx": "^4.20.5"
  }
}
