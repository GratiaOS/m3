# Webhook receiver configuration

# Port to listen on (default: 4001)
PORT=4001

# HMAC secret shared with M3 server
# Must match M3_WEBHOOK_SECRET on the server side
M3_WEBHOOK_SECRET=your-secret-key-here
